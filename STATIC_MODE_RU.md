# Статический Режим - Инструкция

## 🎯 Что изменено

Сайт переведен в **статический режим** без бекенда. Вся аутентификация работает на клиенте через localStorage.

## 🔑 Тестовый аккаунт

Только этот пользователь имеет доступ ко всем курсам:

```
Email: test@test.com
Пароль: 12345
```

## ✅ Как это работает

### Для тестового пользователя (test@test.com):
- ✓ Может войти с паролем `12345`
- ✓ Видит ВСЕ анимационные курсы
- ✓ Доступ ко всем видео курсов
- ✓ Полный функционал аккаунта

### Для других пользователей:
- ✓ Могут зарегистрироваться
- ✓ Могут войти в систему
- ✓ Видят свой профиль
- ✗ **НЕ видят курсы** (показывается сообщение "No courses")

## 📝 Измененные файлы

### 1. `src/contexts/AuthContext.tsx`
- Убраны все вызовы к бекенду
- Добавлена статическая проверка credentials
- Данные сохраняются в localStorage

```typescript
// Проверка логина
if (email === 'test@test.com' && password === '12345') {
  // Успешный вход
} else {
  // Ошибка
}
```

### 2. `src/pages/Account.tsx`
- Курсы показываются только для test@test.com
- Для остальных - сообщение о пустом списке

```typescript
const isTestAccount = user?.email === 'test@test.com';

if (!isTestAccount) {
  return <NoCoursesMessage />;
}

return <AllCoursesWithVideos />;
```

### 3. `.env`
```env
REACT_APP_TEST_USER_EMAIL=test@test.com
REACT_APP_TEST_USER_PASSWORD=12345
REACT_APP_TEST_USER_FIRSTNAME=Test
REACT_APP_TEST_USER_LASTNAME=User
```

## 🧪 Тестирование

### Вариант 1: Тестовый аккаунт
1. Открыть `/login`
2. Ввести: `test@test.com` / `12345`
3. Перейти в Account → вкладка "My Animation Courses"
4. ✅ Все курсы видны с видео

### Вариант 2: Новый пользователь
1. Открыть `/register`
2. Зарегистрировать любой email/пароль
3. Войти с новыми данными
4. Перейти в Account → вкладка "My Animation Courses"
5. ❌ Курсы НЕ видны (сообщение "No courses")

## 💡 Особенности

### Преимущества:
- Работает без сервера
- Быстрая разработка
- Нет затрат на хостинг бекенда
- Можно разместить на статическом хостинге

### Ограничения:
- Только один тестовый пользователь с курсами
- Данные теряются при очистке localStorage
- Нет реальной безопасности
- Не для production использования

## 🔒 Безопасность

⚠️ **Важно**: Это демо-режим!
- Credentials в открытом виде в .env
- Нет шифрования
- Нет защиты данных
- Только для разработки/демо

## 📱 Что работает без изменений

- Login страница
- Register страница  
- Корзина (CartContext)
- Навигация
- Все публичные страницы
- Политики и Terms & Conditions

## 🚀 Запуск

```bash
# Установка зависимостей
npm install

# Запуск
npm start

# Логин с тестовым аккаунтом
Email: test@test.com
Password: 12345
```

## 📊 Структура доступа к курсам

```
Пользователь test@test.com
    └─ Account Page
        └─ My Animation Courses Tab
            ├─ Course 1 ✓ (с видео)
            ├─ Course 2 ✓ (с видео)
            ├─ Course 3 ✓ (с видео)
            └─ ... все курсы

Любой другой пользователь
    └─ Account Page
        └─ My Animation Courses Tab
            └─ "No courses" message ✗
```

## ❓ FAQ

**Q: Могу ли я добавить больше тестовых пользователей с курсами?**  
A: Да, в AuthContext.tsx можно добавить массив тестовых пользователей.

**Q: Как изменить тестового пользователя?**  
A: Измените credentials в .env файле.

**Q: Данные сохраняются?**  
A: Да, в localStorage браузера. Но только до очистки кеша.

**Q: Можно ли вернуть бекенд?**  
A: Да, нужно восстановить оригинальный AuthContext.tsx из git.
